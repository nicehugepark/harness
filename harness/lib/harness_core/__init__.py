"""하네스 공용 라이브러리.

이 패키지는 설계가 인정한 커스텀 잔량 4종(A§5.4 — 생성·착지 도구 / 훅 판정
스크립트 / 배치 잡 / pre-push 스캔)이 공유하는 구현 본체다. 별도 실체를
늘리는 것이 아니라 그 4종의 중복을 없애는 자리이며, 상주 프로세스를 두지
않는다(S§11-19).

정본 주소 표기 규약: 각 모듈 docstring 첫 줄에 자기가 구현하는 계약의 주소를
적는다. 주소 없는 모듈은 어느 계약도 구현하지 않는 것이므로 존재 근거가 없다.
"""

__all__ = [
    "ids",
    "paths",
    "clock",
    "frontmatter",
    "schema",
    "gate",
    "envelope",
    "policy",
]
